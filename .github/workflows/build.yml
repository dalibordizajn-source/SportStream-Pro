name: Build SportStream APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout koda
        uses: actions/checkout@v4

      - name: Postavljanje Java sustava
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Pečenje APK datoteke
        run: echo "Sustav spreman, čeka se ostatak koda..."
